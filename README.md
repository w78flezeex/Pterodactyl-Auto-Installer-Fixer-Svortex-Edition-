# Pterodactyl Auto-Installer & Fixer (Svortex Edition)

Автоматический установщик Pterodactyl Panel и Wings, специально доработанный для VDS хостинга **Svortex.ru**. Скрипт решает проблемы с нестандартными портами, NAT Loopback и настройкой проксирования "из коробки".

> **Автор модификации:** PRD_YT
> **Специально для:** Клиентов Svortex

---

## 🚀 Особенности

Этот скрипт основан на официальном `pterodactyl-installer`, но включает в себя критически важные исправления:

1.  **Выбор портов:** В начале установки скрипт спросит желаемые порты для Панели, Wings API и SFTP (вместо стандартных 80/8080/2022).
2.  **NAT Loopback Fix:** Автоматически настраивает `iptables`, чтобы панель могла подключаться к Wings по внешнему IP адресу (решает ошибку `Gateway Timeout` и проблемы с файловым менеджером).
3.  **Nginx Proxy Fix:** Исправляет конфигурацию Nginx, убирая конфликт с эндпоинтом `remote`, из-за которого Wings не мог получить конфигурацию.
4.  **Auto Firewall:** Автоматически открывает выбранные порты в `ufw` и `iptables`.
5.  **Auto Config:** Обновляет `.env` и `config.yml` с правильными портами и адресами (`0.0.0.0`).

---

## 📦 Установка

Запустите следующую команду на вашем чистом сервере (Ubuntu 20.04/22.04, Debian 10/11):

curl -s https://github.com/w78flezeex/Pterodactyl-Auto-Installer-Fixer-Svortex-Edition-?tab=readme-ov-file#pterodactyl-auto-installer--fixer-svortex-edition

### Процесс установки:
1.  Запустите скрипт.
2.  Введите желаемые порты, когда вас попросят (или нажмите Enter для стандартных).
    *   *Пример для Svortex:* Панель - `2963`, Wings - `25896`, SFTP - `2772`.
3.  Следуйте стандартным инструкциям установщика (ввод email, пароля и т.д.).
4.  **Готово!** Все фиксы применятся автоматически после завершения установки.

---

## ⚠️ Важно для клиентов Svortex

Если вы используете VDS от **Svortex.ru**, вам необходимо выполнить дополнительные действия в панели управления хостингом, чтобы порты были доступны извне:

1.  Зайдите в панель управления Svortex.
2.  Перейдите в раздел **Network (Сеть)** -> **Startup**.
3.  Найдите поле **Additional ports (Дополнительные порты)**.
4.  Добавьте туда все порты, которые вы указали при установке, через запятую (например: `2963, 25896, 2772`).
5.  Перезагрузите сервер через панель управления хостингом, если потребуется.

---

## 🛠 Решение проблем

Если после установки вы видите ошибку **"Something went wrong"** в файловом менеджере или **"Gateway Timeout"**:

1.  Убедитесь, что вы добавили порты в панели Svortex (см. выше).
2.  Проверьте, запущен ли Wings: `systemctl status wings`
3.  Убедитесь, что правило NAT Loopback активно:
    
    iptables -t nat -L OUTPUT -n -v
        Вы должны увидеть правило `DNAT` для вашего внешнего IP.

---

### Disclaimer

Этот скрипт не является официальным продуктом Pterodactyl.
Основано на [pterodactyl-installer/pterodactyl-installer](https://github.com/pterodactyl-installer/pterodactyl-installer).
